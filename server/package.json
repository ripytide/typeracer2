{
	"name": "typeracer2-server",
	"version": "0.0.1",
	"description": "the backend for typeracer2",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"express": "^4.17.1",
		"socket.io": "^4.1.2"
	},
	"type": "module"
}
